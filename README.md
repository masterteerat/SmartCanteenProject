## Compile Java with Library

### Windows (PowerShell / CMD)

```bash
javac -cp ".;lib/*" *.java
```

Run program:

```bash
java -cp ".;lib/*" Tester
```

---

### macOS / Linux

```bash
javac -cp ".:lib/*" *.java
```

Run program:

```bash
java -cp ".:lib/*" Tester
```

---

## Run in VS Code
